# Base de datos
psycopg[binary]>=3.1,<4
python-dotenv>=1.0,<2

# Extracción con LLM (processor.py)
# `messages.parse()` con output_format requiere anthropic >= 0.100
anthropic>=0.105,<1
pydantic>=2.7,<3

# Scraping
# curl_cffi hace impersonation de TLS: es lo que permite pasar el Cloudflare
# de Laborum sin navegador. `requests` normal recibe 403.
curl-cffi>=0.7,<1
apify-client>=1.7,<2
httpx>=0.27,<1
